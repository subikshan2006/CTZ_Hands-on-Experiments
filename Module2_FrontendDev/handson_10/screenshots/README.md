# Screenshots

Add screenshots of the running app here, e.g.:

- `courses_loading.png` — Redux `pending` state (spinner visible)
- `courses_loaded.png` — course grid populated via `fetchAllCourses` thunk
- `courses_error.png` — Redux `rejected` state with Retry button
- `redux_devtools.png` — Redux DevTools showing thunk lifecycle actions
- `error_boundary.png` — fallback UI after a simulated render error
